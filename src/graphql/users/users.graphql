scalar DateTime

type User {
  id: ID!
  first_name: String!
  last_name: String
  email: String
  date_of_birth: DateTime
  created_at: DateTime!
  modified_at: DateTime!
  tags: [Tag!]!
}

type Query {
  # users: [User!]!
  user(id: ID!): User
  # usertags(userId: ID!): [Tag!]!
}
